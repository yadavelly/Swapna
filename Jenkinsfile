pipeline {
   agent any

   stages {
      stage('Hello Git Hook ..') {
         steps {
            echo 'Hello World added git hook to know push events are triggering build or not'
         }
      }
   }
}
